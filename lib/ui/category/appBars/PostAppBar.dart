import 'package:flutter/material.dart';

class PostsAppBar extends StatelessWidget {
  const PostsAppBar({super.key});

  @override
  Widget build(BuildContext context) {
    return AppBar(
      title: Text("PostsAppBar"),
    );
  }
}