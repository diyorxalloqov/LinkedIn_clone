import 'package:flutter/material.dart';

class MyNetworkAppBar extends StatelessWidget {
  const MyNetworkAppBar({super.key});

  @override
  Widget build(BuildContext context) {
    return AppBar(
      title: const Text("MyNetworkAppBar"),
    );
  }
}