import 'package:flutter/material.dart';

class JobsAppBar extends StatelessWidget {
  const JobsAppBar({super.key});

  @override
  Widget build(BuildContext context) {
    return AppBar(
      title: Text("JobsAppBar"),
    );
  }
}